# Assessment service Deployment — adds AUTH_SERVICE_URL and RUN_SEED
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: assessment-service
    {{- include "wellnest.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.assessment.replicaCount }}
  selector:
    matchLabels:
      app: assessment-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: assessment-service
        environment: {{ .Values.global.environment }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: assessment-service
          image: docker.io/{{ .Values.global.dockerUsername }}/{{ .Values.assessment.image.repository }}:{{ .Values.assessment.image.tag }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.global.services.assessment.port }}
              name: http
              protocol: TCP
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: ASSESSMENT_SERVICE_PORT
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: NODE_ENV
            - name: FRONTEND_URL
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: FRONTEND_URL
            - name: JWT_EXPIRY
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: JWT_EXPIRY
            - name: AUTH_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: AUTH_SERVICE_URL
            - name: RUN_SEED
              valueFrom:
                configMapKeyRef:
                  name: wellnest-config
                  key: RUN_SEED
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: wellnest-secrets
                  key: MONGODB_URI
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wellnest-secrets
                  key: JWT_SECRET
          resources:
            {{- toYaml .Values.assessment.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.global.services.assessment.port }}
            initialDelaySeconds: {{ .Values.assessment.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.assessment.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.assessment.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.assessment.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.global.services.assessment.port }}
            initialDelaySeconds: {{ .Values.assessment.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.assessment.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.assessment.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.assessment.probes.readiness.failureThreshold }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
